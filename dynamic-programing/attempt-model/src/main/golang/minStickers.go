package main

func minStickers(stickers []string, target string) int {
       stickBucket := make([][]byte,0,len(stickers))
       for _,sticker:=range stickers{
              stickBucket = append()
       }
}